{
  "name": "mailzip",
  "version": "0.2.0",
  "description": "Thunderbird extension: automatically zip matching attachments before sending",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/thunderbird-webext-browser": "^127.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
